# تصدير أطلس ليبيا السياحي إلى GitHub

## النسخة الجاهزة

النسخة المستقرة التي يجب تصديرها هي checkpoint `b65e0f6b`. تتضمن هذه النسخة الخريطة التفاعلية، الطبقات الجغرافية، الهوية الرسمية، نظام المصادقة، قاعدة بيانات نقاط الأطلس، رفع الصور، الوصف والبيانات الوصفية، واختبارًا فعليًا بحساب مدير.

## المستودع المستهدف

`https://github.com/tidclibya2026/tidcatlas.ly`

## طريقة التصدير المعتمدة

من لوحة إدارة مشروع Manus، افتح **GitHub** ثم اختر **Export code**. استخدم المستودع `tidcatlas.ly` ضمن الحساب `tidclibya2026`، وصدّر النسخة من checkpoint `b65e0f6b`. بعد نجاح التصدير، تحقق من ظهور `package.json` و`client/` و`server/` و`drizzle/` و`shared/` في المستودع.

## ملاحظة الوصول

تم التحقق من أن مصادقة GitHub غير متاحة لأداة سطر الأوامر في بيئة التطوير، لذلك لا يتم تنفيذ `git push` مباشر من داخل المشروع. التصدير من لوحة إدارة المشروع هو المسار الآمن المعتمد، ولا يتطلب إرسال كلمة مرور أو Access Token داخل المحادثة.
