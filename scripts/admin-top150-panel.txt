<section className="mt-6 border-t border-[#d7c9b5] pt-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-bold">أفضل 150 موقعًا — مراجعة تدريجية</h2><p className="mt-1 text-sm text-slate-600">هذه القائمة لا تُنشر تلقائيًا. يراجع الفريق الاسم والإحداثيات والصور والحقوق ثم يعتمد السجل من مسار التوثيق.</p></div><span className="text-sm text-[#b86f3c]">معلق: {top150ReviewQueue.data?.length ?? 0}</span></div><div className="mt-3 flex gap-2"><Input value={top150Search} onChange={(event) => setTop150Search(event.target.value)} placeholder="ابحث في أفضل 150 بالاسم أو المنطقة" /><Button type="button" variant="outline" onClick={() => setTop150Search("")}>مسح</Button></div><div className="mt-3 max-h-[520px] space-y-2 overflow-y-auto">{top150ReviewQueue.isLoading ? <p>جارٍ تحميل قائمة أفضل 150…</p> : top150ReviewQueue.data?.map((row) => <article key={row.rank} className="border border-[#e1d5c4] bg-[#fffdf8] p-3 text-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><strong>#{row.rank} · {row.candidate}</strong><p className="mt-1 text-slate-600">المنطقة: {row.region} · درجة المطابقة: {row.matchScore.toFixed(2)}</p><p className="text-xs text-slate-500">{row.confirmedName ? `الاسم الحالي: ${row.confirmedName}` : "لا يوجد اسم حالي مؤكد — يلزم التحقق اليدوي"}</p></div><span className="border border-[#d7c9b5] px-2 py-1 text-xs text-[#b86f3c]">قيد المراجعة</span></div></article>)}{!top150ReviewQueue.data?.length && <p className="border border-dashed border-[#d7c9b5] p-5 text-center text-slate-500">لا توجد نتائج مطابقة.</p>}</div></section>
