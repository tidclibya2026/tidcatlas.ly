import fs from 'node:fs';
const path='client/src/pages/Home.tsx';
let text=fs.readFileSync(path,'utf8');
const replacements=[
['  const [selectedHeritageCategory, setSelectedHeritageCategory] = useState("all");','  const [selectedHeritageCategory, setSelectedHeritageCategory] = useState("all");\n  const [selectedNaturalCategory, setSelectedNaturalCategory] = useState("all");\n  const [selectedInvestmentCategory, setSelectedInvestmentCategory] = useState("all");\n  const [selectedResortCategory, setSelectedResortCategory] = useState("all");'],
['  const heritageCategoryOptions = useMemo(() => Array.from(new Set(activeSites.filter((site) => site.layerId === "heritage").map((site) => siteValue(site, ["category", "type", "classification", "التصنيف", "Subcategory_التصنيف الفرعي"])).filter(Boolean))).sort((a, b) => a.localeCompare(b, "ar")), [activeSites]);','  const categoryOptionsForLayer = useCallback((layerId: string) => Array.from(new Set(activeSites.filter((site) => site.layerId === layerId).map((site) => siteValue(site, ["category", "type", "classification", "التصنيف", "Subcategory_التصنيف الفرعي", "primary_category", "category_enriched"])).filter(Boolean))).sort((a, b) => a.localeCompare(b, "ar")), [activeSites]);\n  const heritageCategoryOptions = useMemo(() => categoryOptionsForLayer("heritage"), [categoryOptionsForLayer]);\n  const naturalCategoryOptions = useMemo(() => categoryOptionsForLayer("natural"), [categoryOptionsForLayer]);\n  const investmentCategoryOptions = useMemo(() => categoryOptionsForLayer("investment"), [categoryOptionsForLayer]);\n  const resortCategoryOptions = useMemo(() => categoryOptionsForLayer("resorts"), [categoryOptionsForLayer]);'],
['    if (selectedHeritageCategory === "all") return filtered;\n    return filtered.filter((site) => site.layerId !== "heritage" || siteValue(site, ["category", "type", "classification", "التصنيف", "Subcategory_التصنيف الفرعي"]).includes(selectedHeritageCategory));\n  }, [activeSites, query, selectedCategory, selectedMunicipality, selectedLayer, selectedStatus, selectedHeritageCategory]);','    return filtered.filter((site) => {\n      const siteCategory = siteValue(site, ["category", "type", "classification", "التصنيف", "Subcategory_التصنيف الفرعي", "primary_category", "category_enriched"]);\n      if (site.layerId === "heritage" && selectedHeritageCategory !== "all" && !siteCategory.includes(selectedHeritageCategory)) return false;\n      if (site.layerId === "natural" && selectedNaturalCategory !== "all" && !siteCategory.includes(selectedNaturalCategory)) return false;\n      if (site.layerId === "investment" && selectedInvestmentCategory !== "all" && !siteCategory.includes(selectedInvestmentCategory)) return false;\n      if (site.layerId === "resorts" && selectedResortCategory !== "all" && !siteCategory.includes(selectedResortCategory)) return false;\n      return true;\n    });\n  }, [activeSites, query, selectedCategory, selectedMunicipality, selectedLayer, selectedStatus, selectedHeritageCategory, selectedNaturalCategory, selectedInvestmentCategory, selectedResortCategory]);'],
['(query || selectedCategory !== "all" || selectedMunicipality !== "all" || selectedLayer !== "all" || selectedStatus !== "all" || selectedHeritageCategory !== "all")','(query || selectedCategory !== "all" || selectedMunicipality !== "all" || selectedLayer !== "all" || selectedStatus !== "all" || selectedHeritageCategory !== "all" || selectedNaturalCategory !== "all" || selectedInvestmentCategory !== "all" || selectedResortCategory !== "all")'],
['setSelectedStatus("all"); setSelectedHeritageCategory("all"); }} type="button">إعادة ضبط','setSelectedStatus("all"); setSelectedHeritageCategory("all"); setSelectedNaturalCategory("all"); setSelectedInvestmentCategory("all"); setSelectedResortCategory("all"); }} type="button">إعادة ضبط'],
['{activeLayers.includes("heritage") && heritageCategoryOptions.length > 0 && <label className="heritage-category-filter">تصنيف المواقع التاريخية<select value={selectedHeritageCategory} onChange={(event) => setSelectedHeritageCategory(event.target.value)}><option value="all">كل التصنيفات التاريخية</option>{heritageCategoryOptions.map((category) => <option key={category} value={category}>{category}</option>)}</select></label>}','{activeLayers.includes("heritage") && heritageCategoryOptions.length > 0 && <label className="heritage-category-filter">تصنيف مواقع التراث العالمي<select value={selectedHeritageCategory} onChange={(event) => setSelectedHeritageCategory(event.target.value)}><option value="all">كل التصنيفات التاريخية</option>{heritageCategoryOptions.map((category) => <option key={category} value={category}>{category}</option>)}</select></label>}{activeLayers.includes("natural") && naturalCategoryOptions.length > 0 && <label className="heritage-category-filter">تصنيف المواقع الطبيعية<select value={selectedNaturalCategory} onChange={(event) => setSelectedNaturalCategory(event.target.value)}><option value="all">كل المواقع الطبيعية</option>{naturalCategoryOptions.map((category) => <option key={category} value={category}>{category}</option>)}</select></label>}{activeLayers.includes("investment") && investmentCategoryOptions.length > 0 && <label className="heritage-category-filter">تصنيف فرص الاستثمار<select value={selectedInvestmentCategory} onChange={(event) => setSelectedInvestmentCategory(event.target.value)}><option value="all">كل فرص الاستثمار</option>{investmentCategoryOptions.map((category) => <option key={category} value={category}>{category}</option>)}</select></label>}{activeLayers.includes("resorts") && resortCategoryOptions.length > 0 && <label className="heritage-category-filter">تصنيف القرى والمنتجعات<select value={selectedResortCategory} onChange={(event) => setSelectedResortCategory(event.target.value)}><option value="all">كل القرى والمنتجعات</option>{resortCategoryOptions.map((category) => <option key={category} value={category}>{category}</option>)}</select></label>'],
];
for (const [from,to] of replacements) { if (!text.includes(from)) throw new Error(`Missing target: ${from.slice(0,80)}`); text=text.replace(from,to); }
fs.writeFileSync(path,text);
